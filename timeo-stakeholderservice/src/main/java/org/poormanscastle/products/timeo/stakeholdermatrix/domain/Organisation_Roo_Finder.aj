// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.poormanscastle.products.timeo.stakeholdermatrix.domain;

import javax.persistence.EntityManager;
import javax.persistence.TypedQuery;
import org.poormanscastle.products.timeo.stakeholdermatrix.domain.Organisation;

privileged aspect Organisation_Roo_Finder {
    
    public static Long Organisation.countFindOrganisationsByNameEquals(String name) {
        if (name == null || name.length() == 0) throw new IllegalArgumentException("The name argument is required");
        EntityManager em = Organisation.entityManager();
        TypedQuery q = em.createQuery("SELECT COUNT(o) FROM Organisation AS o WHERE o.name = :name", Long.class);
        q.setParameter("name", name);
        return ((Long) q.getSingleResult());
    }
    
    public static TypedQuery<Organisation> Organisation.findOrganisationsByNameEquals(String name) {
        if (name == null || name.length() == 0) throw new IllegalArgumentException("The name argument is required");
        EntityManager em = Organisation.entityManager();
        TypedQuery<Organisation> q = em.createQuery("SELECT o FROM Organisation AS o WHERE o.name = :name", Organisation.class);
        q.setParameter("name", name);
        return q;
    }
    
    public static TypedQuery<Organisation> Organisation.findOrganisationsByNameEquals(String name, String sortFieldName, String sortOrder) {
        if (name == null || name.length() == 0) throw new IllegalArgumentException("The name argument is required");
        EntityManager em = Organisation.entityManager();
        StringBuilder queryBuilder = new StringBuilder("SELECT o FROM Organisation AS o WHERE o.name = :name");
        if (fieldNames4OrderClauseFilter.contains(sortFieldName)) {
            queryBuilder.append(" ORDER BY ").append(sortFieldName);
            if ("ASC".equalsIgnoreCase(sortOrder) || "DESC".equalsIgnoreCase(sortOrder)) {
                queryBuilder.append(" ").append(sortOrder);
            }
        }
        TypedQuery<Organisation> q = em.createQuery(queryBuilder.toString(), Organisation.class);
        q.setParameter("name", name);
        return q;
    }
    
}
